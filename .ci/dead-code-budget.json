{
  "max": 78,
  "baselineDate": "2026-08-02",
  "scope": "knip findings across src/**, api/**, lib/**, scripts/** and package.json dependencies",
  "note": "REPORT-ONLY. max is the 2026-08-02 baseline (issue #2677), so the check never fails today and the first large report cannot turn into an unrelated cleanup PR. Promotion to blocking is a config change only: triage the list, lower max, and add `pnpm run deadcode:budget` as a required step in .github/workflows/test.yml. Per AGENTS.md, pre-existing dead code is reported rather than deleted."
}
