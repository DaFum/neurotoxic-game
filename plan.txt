1. We need to pass `assetProceeds` to `checkSoftlock` inside `src/hooks/travel/useTravelEffects.ts`.
   To do this we calculate the maximum net positive sale proceeds for all owned assets:
   ```typescript
   import { calculateChassisGrossSaleValue } from '../../utils/assetSelectors/assetFinancials'
   import { finiteNumberOr } from '../../utils/finiteNumber'
   ...
   let maxAssetProceeds = 0
   if (assets) {
     for (const asset of assets) {
       const gross = calculateChassisGrossSaleValue(asset, player.day)
       if (gross !== null) {
         let rawTotalPrincipalRemaining = 0
         if (liabilities) {
           for (const l of Object.values(liabilities)) {
             if (l && l.assetId === asset.id) {
               rawTotalPrincipalRemaining += Math.max(
                 0,
                 finiteNumberOr(l.principalRemaining, 0)
               )
             }
           }
         }
         if (gross >= rawTotalPrincipalRemaining) {
           const net = gross - rawTotalPrincipalRemaining
           if (net > maxAssetProceeds) {
             maxAssetProceeds = net
           }
         }
       }
     }
   }
   ```
   Add this `maxAssetProceeds` logic and add it as `assetProceeds: maxAssetProceeds` to `softlockContext` in `src/hooks/travel/useTravelEffects.ts`.
2. Update tests if necessary to ensure `checkSoftlock` isn't incorrectly modified again.
