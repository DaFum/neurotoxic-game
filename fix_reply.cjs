const fs = require('fs');

console.log(`Ah! Codeant-AI PR comments are sticky! If the bot opened a review thread, it needs to be explicitly marked as resolved or it will keep failing the 'Aggregate Comments & Enforce Resolution' job!`);
