1. **API Validation Security**: In `api/leaderboard/stats.js` and `api/leaderboard/song.js`, add checks for prototype pollution on `req.body` using `Object.hasOwn(req.body, '__proto__')` before processing data.
2. **O(N) Optimization**: In `src/context/reducers/questReducer.js` and other reducers, replace array `.find()` inside high-frequency paths with maps or set lookups if needed, or fix `Object.keys().reduce` in `src/utils/traitUtils.js`. Specifically, in `src/utils/traitUtils.js` change `Object.keys(CHARACTERS).reduce` to use `for...in` and `Object.hasOwn`.
3. **State Integrity**: In `src/context/reducers/gigReducer.js` and `questReducer.js`, ensure all state bounds are strictly precalculated before applying to state (e.g. using `clampPlayerMoney` or `clampBandHarmony`). Ensure that toasts reflect applied delta, not requested delta.
4. **Action Creators**: Enforce that components do not call reducers directly. (This might just mean auditing if `dispatch` is being used properly and verifying reducers are exported/used via action creators).
