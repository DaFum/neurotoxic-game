Let's update the plan:
1. Use `replace_with_git_merge_diff` on `src/utils/assetSelectors.ts` to implement the `EMPTY_LIABILITIES` fallback mechanism as suggested by the code review.
2. Run format, lint, and test scripts to verify the build.
3. Submit the changes.
