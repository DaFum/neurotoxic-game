1. **Explore `src/components/hud/CorruptionMeter.tsx` (Complete)**
   - Identified that `CorruptionMeter` is undocumented and doesn't follow strict TSDoc format.
2. **Apply TSDoc to `src/components/hud/CorruptionMeter.tsx`**
   - Add proper TSDoc tags (`@summary`, `@remarks`, `@param`, `@returns`) following the rules for `CorruptionMeter` and `CorruptionMeterProps`.
3. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
4. **Append the execution record to the ledger**
   - Add exactly one new row to `/.jules/tsdoc.md` with the updated file using UTC date format `| [YYYY-MM-DD] | [File Path] | [Comma-separated list of functions/classes/interfaces updated] |`.
5. **Submit the PR**
   - Use `submit` to push changes.
