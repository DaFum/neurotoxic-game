interface:
  display_name: 'Min Repro Builder'
  short_description: 'create minimal reproduction cases for bugs. Trigger when asked to isolate an issue, create a test case, or debug a complex interaction.'
  default_prompt: 'Build a minimal reproduction that isolates the bug and captures exact steps, inputs, and failing behavior. Return: concise diagnosis, concrete implementation steps, and verification commands.'
