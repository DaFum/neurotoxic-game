interface:
  display_name: 'One-Command Doctor'
  short_description: 'Run a quick healthcheck and summarize likely fixes.'
  default_prompt: 'Run environment diagnostics (tooling, deps, build) and identify the fastest path to a healthy setup. Return: concise diagnosis, concrete implementation steps, and verification commands.'
