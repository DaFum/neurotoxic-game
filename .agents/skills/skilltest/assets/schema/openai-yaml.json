{
  "interfaceFields": [
    "display_name",
    "short_description",
    "icon_small",
    "icon_large",
    "brand_color",
    "default_prompt"
  ],
  "dependenciesFields": ["tools"],
  "toolSchema": {
    "type": "string (e.g. 'mcp')",
    "value": "string (tool identifier)",
    "description": "string",
    "transport": "string (e.g. 'streamable_http')",
    "url": "string (tool endpoint URL)"
  },
  "notes": "Validate file paths for icon_small/icon_large and hex color format (#RRGGBB or #RGB) for brand_color when present.",
  "spec": "https://developers.openai.com/codex/skills"
}
