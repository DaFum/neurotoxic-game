{
  "required": ["name", "description"],
  "optional": ["license", "compatibility", "metadata", "allowed-tools"],
  "nameConstraints": {
    "maxLength": 64,
    "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
    "rules": [
      "Lowercase letters, numbers, and hyphens only",
      "Must not start or end with a hyphen",
      "Must not contain consecutive hyphens",
      "Must match the parent directory name"
    ]
  },
  "descriptionConstraints": {
    "maxLength": 1024,
    "rules": [
      "Must describe what the skill does and when to use it",
      "Should include keywords that help agents identify relevant tasks"
    ]
  },
  "bodyGuidelines": {
    "recommendedMaxLines": 500,
    "recommendedMaxTokens": 5000,
    "notes": "Keep SKILL.md concise. Move detailed reference material to references/ directory."
  },
  "spec": "https://agentskills.io/specification"
}
