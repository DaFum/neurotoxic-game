interface:
  display_name: 'Perf Budget Enforcer'
  short_description: 'optimize bundle size and runtime performance. Trigger when the app is slow, bundle is too large, or build warnings appear. Checks lazy loading, asset sizes, and render loops.'
  default_prompt: 'Profile bundle/runtime costs and enforce performance budgets with concrete optimization actions. Return: concise diagnosis, concrete implementation steps, and verification commands.'
