# Audit Findings Report (Custom Analysis Tools)

This report aggregates the findings generated by the custom audit tools executed from `/home/jules/self_created_tools/`.

---

## 1. i18n Key Discrepancy Audit (`i18n_key_checker.py`)

* **Scope:** Cross-referenced JSON translation key trees between `public/locales/en/` and `public/locales/de/`.
* **Summary:**
  * Total Missing Keys in DE: `0`
  * Total Missing Keys in EN: `0`
* **Status:** ✅ **100% Parity**. All translation keys are fully aligned across German and English namespaces.

---

## 2. Tailwind & Custom CSS Class Audit (`tailwind_unused_checker.py`)

* **Scope:** Scanned `src/**/*.tsx` / `src/**/*.jsx` for `className` tokens and cross-checked against CSS definitions in `src/*.css`.
* **Summary:**
  * Total Custom CSS Classes Defined: `209`
  * Total Unique Class Tokens Used in JSX/TSX: `82`
  * Potentially Unused Custom CSS Classes: `206`
* **Key Observations:**
  * Many unused custom CSS classes stem from legacy style definitions or numeric utility classes (e.g., `.01`, `.02`, `.25rem`).
* **Recommendation:** Review `src/index.css` and `src/overworld.css` during maintenance cycles to prune dead style declarations.

---

## 3. Game State Sanitizer & Boundary Audit (`game_state_simulator.py`)

* **Scope:** Inspected state handling and logic files in `src/utils/` for unsafe raw nullish coalescing (`??`) on numeric values instead of using `finiteNumberOr`.
* **Summary:**
  * Total potential numeric sanitization anti-patterns found: `13`
* **Detected Candidates:**
  1. `src/utils/brandDealLogic.ts:282` -> `const isFamous = (gameState.player?.fame ?? 0) > 1000`
  2. `src/utils/gigVisualStatus.ts:14` -> `const health = stats.health ?? 100`
  3. `src/utils/travelUtils.ts:270` -> `if (clampPlayerMoney(player.money ?? 0) < totalCashRequired)`
  4. `src/utils/unlockCheck.ts:289` -> `predicate: ({ state }) => (state.band.harmony ?? 1) >= 90`
  5. `src/utils/purchaseLogicUtils.ts:627` -> `((nextPlayerPatch.fame as number | undefined) ?? player.fame ?? 0) + 10`
  6. `src/utils/purchaseLogicUtils.ts:679` -> `((nextPlayerPatch.money as number | undefined) ?? player.money ?? 0) +`
  7. `src/utils/socialEngine.ts:142` -> `if (opt.id === 'comm_crowdfund' && (gameState.player.money ?? 0) < 100)`
  8. `src/utils/eventEngine/eventSelection.ts:247` -> `(gameState.band?.harmony ?? 100) < HARMONY_DEATH_SPIRAL_THRESHOLD`
  9. `src/utils/postGig/socialResolution.ts:340` -> `let nextMoney = player.money ?? 0`
  10. `src/utils/postGig/socialResolution.ts:344` -> `player.money ?? 0,`
* **Recommendation:** Replace raw `??` with `finiteNumberOr(val, fallback)` to prevent `NaN` or non-finite propagation.
* **Code Verification Note:** Line 282 of `src/utils/brandDealLogic.ts` (`const isFamous = (gameState.player?.fame ?? 0) > 1000`) was manually verified against the codebase.

---

## 4. Audio Node Safety & Memory Leak Audit (`audio_asset_checker.py`)

* **Scope:** Scanned `src/` for object spread anti-patterns (`{ ...node }` or `Object.assign({}, node)`) on WebAudio and Tone.js sound instances.
* **Summary:**
  * Total Audio Node Spread Anti-Patterns Found: `0`
* **Status:** ✅ **Safe**. No Tone.js or Web Audio API nodes were shallow-copied using object spread in the core audio architecture.

---

## 5. Garbage Collection & Hot-Path Performance Audit (`gc_pressure_analyzer.py`)

* **Scope:** Analyzed high-frequency helper paths in `src/utils/` for array-allocating methods (`.map()`, `.filter()`, `Object.values()`).
* **Summary:**
  * Total Allocating Functional Calls in `src/utils/`: `54`
* **Sample Candidates for Procedural Loop Refactoring:**
  * `src/utils/saveValidator.ts:351` -> `const survivingDeals = (val as unknown[]).filter(...)`
  * `src/utils/saveValidator.ts:382` -> `const entries = Object.entries(val)`
  * `src/utils/brandDealLogic.ts:238` -> `return picked.map(entry => ...)`
  * `src/utils/objectUtils.ts:107` -> `return value.filter(...)`
  * `src/utils/objectUtils.ts:370` -> `for (const nested of Object.values(value))`
  * `src/utils/traitUtils.ts:278` -> `const nextMembers: MemberWithTraits[] = members.map(...)`
  * `src/utils/mapUtils.ts:240` -> `van.upgrades.filter(...)`
* **Recommendation:** Convert hot-path array iterations into pre-allocated `for` loops where performance/framerate is critical.
