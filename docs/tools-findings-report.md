# Audit Findings Report (Custom Analysis Tools)

This report aggregates the reproducible findings generated by the custom audit tools executed from `scripts/custom_tools/`.

---

## 1. i18n Key Discrepancy Audit (`i18n_key_checker.py`)

* **Scope:** Cross-referenced JSON translation key trees between `public/locales/en/` and `public/locales/de/`. Robust exit handling ensures JSON parse failures throw non-zero exit codes.
* **Summary:**
  * Total Missing Keys in DE: `0`
  * Total Missing Keys in EN: `0`
* **Status:** ✅ **100% Parity**. All translation keys are fully aligned across German and English namespaces.

---

## 2. Tailwind & Custom CSS Class Audit (`tailwind_unused_checker.py`)

* **Scope:** Scanned `src/**/*.tsx` / `src/**/*.jsx` for `className` tokens (including template literals `className={`...`}`) and cross-checked against CSS class selector definitions in `src/*.css` (excluding comments and numeric/measurement literals).
* **Summary:**
  * Total Custom CSS Classes Defined: `143`
  * Total Unique Class Tokens Used in JSX/TSX: `996`
  * Potentially Unused Custom CSS Classes: `56`
* **Key Observations:**
  * Unused custom CSS class candidates are strictly class selectors (e.g. `.animate-confirm-pulse`, `.band-hdr`, `.bar-fill`) rather than numeric unit false positives.
* **Recommendation:** Review `src/index.css` and `src/overworld.css` during maintenance cycles to prune dead style declarations.

---

## 3. Game State Sanitizer & Boundary Audit (`game_state_simulator.py`)

* **Scope:** Inspected state handling files in `src/utils/` for unsafe raw nullish coalescing (`??`) on numeric values during stored state mutations/assignments instead of using `finiteNumberOr`. Ignores read-only condition checks.
* **Summary:**
  * Total potential numeric sanitization anti-patterns found: `1`
* **Detected Candidates:**
  1. `src/utils/postGig/dealHandlers.ts:36` -> `let nextMoney = player.money ?? 0`
* **Recommendation:** Replace raw assignment `??` with `finiteNumberOr(val, fallback)` on state update paths to prevent `NaN` or non-finite propagation.

---

## 4. Audio Node Safety & Memory Leak Audit (`audio_asset_checker.py`)

* **Scope:** Scanned `src/` for object spread anti-patterns (`{ ...node }` or `Object.assign({}, node)`) on WebAudio and Tone.js sound instances specifically (excluding generic `player` / `van` game state objects).
* **Summary:**
  * Total Audio Node Spread Anti-Patterns Found: `0`
* **Status:** ✅ **Safe & Reproducible**. Running `python3 scripts/custom_tools/audio_asset_checker.py` reports `0` findings. No Tone.js or Web Audio API nodes are shallow-copied using object spread in the audio architecture.

---

## 5. Garbage Collection & Hot-Path Performance Audit (`gc_pressure_analyzer.py`)

* **Scope:** Analyzed high-frequency helper paths in `src/utils/` for array-allocating methods (`.map()`, `.filter()`, `Object.values()`), ignoring comments.
* **Summary:**
  * Total Allocating Functional Calls in `src/utils/`: `36`
* **Sample Candidates for Procedural Loop Refactoring:**
  * `src/utils/translationUtils.ts:39` -> `return context.map(item => ...)`
  * `src/utils/unlockCheck.ts:128` -> `const songs = songStats.flatMap(...)`
  * `src/utils/logger.ts:118` -> `this.listeners = this.listeners.filter(...)`
  * `src/utils/storage.ts:237` -> `const persisted = adapter.keys().filter(...)`
  * `src/utils/balanceTuning.ts:189` -> `Object.entries(overrides.earlyGame ?? {})`
  * `src/utils/mapValidation.ts:235` -> `const nodeList = Object.values(nodes)`
  * `src/utils/mapValidation.ts:398` -> `const startNodes = Object.values(nodes).filter(...)`
* **Recommendation:** Convert hot-path array iterations into pre-allocated `for` loops where performance/framerate is critical.
