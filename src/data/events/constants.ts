/**
 * UI Strings and constants for the event system.
 *
 * @remarks
 * These are legacy hardcoded fallback fragments, not namespaced i18n keys. New
 * user-facing event copy should use locale resources instead.
 */
export const EVENT_STRINGS = {
  SAVED_BY_BANDLEADER: ' (Saved by Bandleader!)'
}
