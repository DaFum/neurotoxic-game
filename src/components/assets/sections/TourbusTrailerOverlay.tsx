import type { LongTermAsset } from '../../../types/assets'

interface Props {
  asset: LongTermAsset
  onSlotClick: (slotId: string) => void
}

// Placeholder — full implementation in Plan 2 Task 5.
export const TourbusTrailerOverlay = (_: Props) => null
