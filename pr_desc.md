🎯 **What:** Extracted the massive monolithic sub-calculations inside `gigLogic.ts` into isolated, pure functions within a new `src/utils/economy/gigLogic/calculators/` directory, resolving the code health issue of extensive logic in a single file.
💡 **Why:** By extracting specific sub-calculations (e.g. `calculateTicketIncome`, `calculateMerchIncome`, `calculateVenueSplit`) into their own files, we can perform focused unit testing on the economy engine without importing the entire monolithic logic. It also vastly improves the maintainability and readability of the main `calculateGigFinancials` function.
✅ **Verification:** Verified by running all unit, UI, and logic tests (`pnpm test:vitest:node`, `pnpm test:vitest:logic`, `pnpm test:vitest:ui`), `pnpm run lint --fix` for clean imports, and `pnpm run typecheck:core` to ensure no TypeScript regressions. The code executes identical math, ensuring functionality is safely preserved.
✨ **Result:** A fully decoupled `gigLogic` module that retains the exact same business logic while separating concerns into highly maintainable, testable components.
